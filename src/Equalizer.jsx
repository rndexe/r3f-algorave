import { useRef, useEffect } from 'react';
import { useAudio } from './AudioProvider.jsx'


function EqualizerGraph () {
  const {low,mid,high, amplitude} = useAudio(); // Hook providing low, mid, and high (replace with actual implementation)


  return (
  );
}

export default EqualizerGraph;

